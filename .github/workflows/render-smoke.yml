name: UNMINDY Render Smoke
on:
  push:
    paths:
      - '.github/workflows/render-smoke.yml'
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Smoke test
        run: echo "Render workflow runner is alive"
      # second-commit trigger
