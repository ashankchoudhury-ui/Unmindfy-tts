import { GoogleGenAI } from '@google/genai';
import { createClient } from '@supabase/supabase-js';
const SUPABASE_URL='https://iwpanewluzilghoitvxr.supabase.co';
const RECORD_ID='f6c1e7a4-8b32-4d59-91e6-2c7a5f0b3d84';
const SCRIPT=`You could pause time. Imagine finding a moment you loved and never having to leave it. But if you could stop every moment… what would make any moment special?`;
function pcmToWav(pcm,sampleRate=24000,channels=1,bitsPerSample=16){const byteRate=sampleRate*channels*bitsPerSample/8,blockAlign=channels*bitsPerSample/8,b=Buffer.alloc(44+pcm.length);b.write('RIFF',0);b.writeUInt32LE(36+pcm.length,4);b.write('WAVE',8);b.write('fmt ',12);b.writeUInt32LE(16,16);b.writeUInt16LE(1,20);b.writeUInt16LE(channels,22);b.writeUInt32LE(sampleRate,24);b.writeUInt32LE(byteRate,28);b.writeUInt16LE(blockAlign,32);b.writeUInt16LE(bitsPerSample,34);b.write('data',36);b.writeUInt32LE(pcm.length,40);pcm.copy(b,44);return b}
function naturalize(pcm,sampleRate=24000){const frame=120,fb=240,threshold=500,min=Math.floor(sampleRate*.14),max=Math.floor(sampleRate*.24),out=[];let c=0;const silent=o=>{const e=Math.min(o+fb,pcm.length);let s=0,n=0;for(let p=o;p+1<e;p+=2){s+=Math.abs(pcm.readInt16LE(p));n++}return n>0&&s/n<threshold};while(c<pcm.length){if(!silent(c)){out.push(pcm.subarray(c,Math.min(c+fb,pcm.length)));c+=fb;continue}const st=c;while(c<pcm.length&&silent(c))c+=fb;const samples=Math.floor((Math.min(c,pcm.length)-st)/2);out.push(samples<min?pcm.subarray(st,Math.min(c,pcm.length)):pcm.subarray(st,st+Math.min(samples,max)*2))}return Buffer.concat(out)}
export default async function handler(req,res){try{if(req.method!=='GET'&&req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(req.method==='POST'&&req.headers.authorization!==`Bearer ${process.env.CRON_SECRET}`)return res.status(401).json({error:'Unauthorized'});const key=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SECRET_KEY;if(!key||!process.env.GEMINI_API_KEY)throw new Error('Missing required environment variable');const supabase=createClient(SUPABASE_URL,key),ai=new GoogleGenAI({apiKey:process.env.GEMINI_API_KEY});const prompt=`UNMINDY REEL #16 — PAUSING TIME — TTS AUDIO ONLY. Voice Algieba. Naturally deep, calm, conversational, slightly dry/deadpan, subtly expressive and intimate. Sound like a real person casually talking to one person, not a narrator. Think the script, don't perform it. Subtle progression: possibility → warmth → realization. Use tiny natural conversational gaps between thoughts, around 0.15–0.22 seconds. Do NOT make it gapless, but also do NOT create long cinematic pauses. Keep the whole narration flowing as one natural thought. Brief pause after the hook; tiny pause after “loved”; smooth continuation into “and never having to leave it”; understated turn on “But”; brief thinking beat at the ellipsis; slightly slower genuine final question. No dramatic emphasis, breaths, fillers, stutters, extra words, music, SFX, intro, or outro. Preserve exact wording. SCRIPT: ${SCRIPT}`;await supabase.from('content_pipeline').update({tts_status:'Generating',tts_attempts:1,tts_started_at:new Date().toISOString()}).eq('id',RECORD_ID);const response=await ai.models.generateContent({model:'gemini-3.1-flash-tts-preview',contents:[{role:'user',parts:[{text:prompt}]}],config:{responseModalities:['AUDIO'],speechConfig:{voiceConfig:{prebuiltVoiceConfig:{voiceName:'Algieba'}}}}});const part=response.candidates?.[0]?.content?.parts?.find(p=>p.inlineData?.data);if(!part)throw new Error('No audio returned by Gemini');const wav=pcmToWav(naturalize(Buffer.from(part.inlineData.data,'base64'))),path=`tts/${RECORD_ID}.wav`;const {error:uploadError}=await supabase.storage.from('tts-audio').upload(path,wav,{contentType:'audio/wav',upsert:true,cacheControl:'0'});if(uploadError)throw uploadError;const {data:publicData}=supabase.storage.from('tts-audio').getPublicUrl(path),url=`${publicData.publicUrl}?v=${Date.now()}`;const {error:updateError}=await supabase.from('content_pipeline').update({tts_status:'Ready',tts_audio_url:url,updated_at:new Date().toISOString()}).eq('id',RECORD_ID);if(updateError)throw updateError;return res.status(200).json({ok:true,status:'Ready',url})}catch(error){console.error('UNMINDY Reel #16 TTS error:',error);return res.status(500).json({error:error instanceof Error?error.message:String(error)})}}